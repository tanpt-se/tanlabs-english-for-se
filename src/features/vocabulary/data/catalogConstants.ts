/** Local-preview list cap only (`VOCABULARY_FORCE_LOCAL_SEED`). Remote browse is unpaged per situation. */
export const VOCABULARY_PREVIEW_LIST_LIMIT = 120;
export const VOCABULARY_PRACTICE_QUESTION_COUNT = 20;
/** Matches PostgREST `max_rows` in `supabase/config.toml`. */
export const VOCABULARY_REMOTE_PAGE_SIZE = 1000;
